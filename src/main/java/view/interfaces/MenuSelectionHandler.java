package view.interfaces;

/**
 * Interface for handling menu selections in the application.
 */
public interface MenuSelectionHandler {

    /**
     * Handles the selection of a menu item.
     *
     * @param menuId the identifier of the selected menu
     */
    void selectMenu(String menuId);
}
